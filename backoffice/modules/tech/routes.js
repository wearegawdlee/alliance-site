const express=require('express');const service=require('./service');const router=express.Router();router.get('/dashboard',async(req,res,next)=>{try{res.render('tech/dashboard',{title:'Technician Dashboard',dashboard:await service.getTechnicianDashboard(req.session.user.id)});}catch(e){next(e);}});router.get('/',(req,res)=>res.redirect(req.app.locals.routePath('/tech/dashboard')));module.exports=router;
